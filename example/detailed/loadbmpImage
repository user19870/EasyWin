#include <easywingui.h>
using namespace easywingui;
easyw c;
int main(){
    c.creatw(L"ICON",800,600);
    c.staticImage(L"test.bmp",50,50,200,200);c.staticImage(L"test2.bmp",300,50,200,200);
    while(1){
    	c.runMessage(nullptr);Sleep(1);
	}
    return 0;
}
